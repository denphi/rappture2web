# Extra dependencies for the visual/layout test suite (tests/visual/).
#
# Setup:
#   python3 -m venv .venv
#   .venv/bin/pip install -e ".[dev]" -r requirements-visual.txt
#   .venv/bin/playwright install chromium
#
# Run:
#   .venv/bin/pytest tests/visual/ -v
playwright>=1.60
pytest-playwright>=0.8
